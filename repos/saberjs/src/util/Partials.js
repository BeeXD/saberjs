'use strict';

const { constructedEnumerable } = require('./Enum');

module.exports = constructedEnumerable([
    'User',
    'Channel',
    'GuildMember',
    'Message',
    'Reaction',
    'ScheduledEvent',
    'ThreadMember'
])